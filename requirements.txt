fastapi>=0.115.0
uvicorn[standard]>=0.29.0

# MCP (Model Context Protocol) SSE server
# Pinned below 2.0 — 2.0.0 renamed/removed the low-level Server.list_tools()
# decorator API that src/api/mcp_sse.py depends on ("'Server' object has no
# attribute 'list_tools'"). Re-evaluate the mcp_sse.py implementation against
# 2.x's new API before lifting this cap.
mcp>=1.20.0,<2.0.0

# LLM Providers
anthropic>=0.27.0
google-generativeai>=0.3.0
google-cloud-aiplatform>=1.38.0

# Database
trino==0.328.0
ldap3>=2.9.1                   # Optional LDAP auth for admin/assistant login
psycopg2-binary>=2.9.9        # PostgreSQL
mysql-connector-python>=8.2.0  # MySQL
cryptography>=43.0.0           # CVE fixes from <42 series
sqlglot>=25.0.0                # SQL parsing for MCP mask-rule column resolution

# Core
pydantic>=2.0.0
pyyaml==6.0.1
python-dotenv==1.0.0
ruamel.yaml>=0.18.0
python-multipart>=0.0.18       # CVE-2024-53981 DoS fix

# Scheduled Queries
APScheduler>=3.10.4            # Background job scheduling
openpyxl>=3.1.2
asyncpg                        #Excel file generation

google-cloud-bigquery>=3.11.0
google-auth>=2.23.0

# Testing
pytest>=8.0.0

